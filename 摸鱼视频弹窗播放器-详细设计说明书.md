# 摸鱼视频弹窗播放器（Windows 桌面版）详细设计说明书

> 版本：v1.0
> 文档状态：设计稿（待评审）
> 目标平台：Windows 10 1809+ / Windows 11（x64）
> 技术路线：C# (.NET 8) + WPF + Microsoft WebView2

---

## 目录

1. [项目概述](#1-项目概述)
2. [需求分析](#2-需求分析)
3. [技术选型](#3-技术选型)
4. [系统架构设计](#4-系统架构设计)
5. [核心机制详细设计](#5-核心机制详细设计)
6. [界面设计](#6-界面设计)
7. [视频源适配器设计](#7-视频源适配器设计)
8. [数据存储设计](#8-数据存储设计)
9. [状态机设计](#9-状态机设计)
10. [关键流程时序设计](#10-关键流程时序设计)
11. [安全与合规](#11-安全与合规)
12. [性能与资源占用规范](#12-性能与资源占用规范)
13. [测试计划](#13-测试计划)
14. [开发计划与里程碑](#14-开发计划与里程碑)
15. [风险识别与应对](#15-风险识别与应对)
16. [附录：接口定义、热键表与配置示例](#16-附录接口定义热键表与配置示例)

---

## 1. 项目概述

### 1.1 项目背景

「摸鱼视频弹窗播放器」是一款 Windows 桌面常驻小工具。它以**系统右下角广告弹窗**的形态呈现：平时循环轮播"广告"画面（伪装态），当鼠标悬停到窗口上时，切换为在线视频播放（抖音、哔哩哔哩短视频，腾讯视频、爱奇艺长视频等）；鼠标移开后视频立即暂停并恢复广告轮播，从而在办公环境下实现"看起来像广告弹窗"的低感知娱乐体验。

### 1.2 产品定位

| 维度 | 说明 |
| --- | --- |
| 形态 | 无边框小窗（默认 400×225，16:9），停靠桌面右下角、任务栏上方 |
| 伪装 | 广告轮播（本地假广告素材，带假关闭按钮、假倒计时），无任务栏图标、不进 Alt+Tab |
| 交互 | 悬停即播、移开即停；全局热键切换视频/暂停/显隐 |
| 范围 | 仅做"播放器外壳 + 官方嵌入播放器"，不做破解、不去除平台广告、不下载视频 |

### 1.3 设计目标

| 目标 | 指标 |
| --- | --- |
| 状态切换速度 | 广告 ↔ 视频切换 ≤ 150ms；老板键隐藏 ≤ 100ms |
| 资源占用 | 常驻内存（含 WebView2 子进程）≤ 250MB，空闲 CPU ≤ 2% |
| 隐蔽性 | 任务栏无图标、Alt+Tab 不可见、窗口标题/托盘可伪装 |
| 稳定性 | 崩溃静默重启，配置/队列多级兜底 |
| 可扩展 | 视频源以"适配器"插件化，新增平台不需改动核心 |

---

## 2. 需求分析

### 2.1 功能需求清单

| 编号 | 需求 | 优先级 | 说明 |
| --- | --- | --- | --- |
| F01 | 右下角弹窗窗口 | P0 | 无边框、默认置顶可关、停靠右下角、可拖拽、位置记忆 |
| F02 | 广告轮播伪装层 | P0 | 鼠标不在播放区时轮播本地广告素材（淡入淡出、假关闭按钮、假进度点） |
| F03 | 悬停播放 | P0 | 鼠标进入窗口 → 显示视频并播放；移开 → 暂停并切回广告 |
| F04 | 视频源支持 | P0 | 哔哩哔哩（官方嵌入）、腾讯视频（官方嵌入）、自定义网页 URL |
| F05 | 视频源扩展 | P1 | 抖音、爱奇艺（网页适配）；斗鱼/抖音直播（P2） |
| F06 | 全局热键 | P0 | 上一个/下一个/播放暂停/音量/老板键，窗口未聚焦时也生效 |
| F07 | 老板键 | P0 | 一键隐藏窗口（或一键切回广告态），再按恢复 |
| F08 | 播放队列管理 | P1 | 列表增删、排序、分类标签、上次播放位置记忆 |
| F09 | 播放历史与收藏 | P1 | 本地记录，可一键加入队列 |
| F10 | 登录态支持 | P1 | 隐藏登录窗口（WebView2 共享 Cookie），支持登录后看 VIP/关注内容 |
| F11 | 设置界面 | P1 | 窗口尺寸/透明度/热键/悬停延时/广告目录/开机自启 |
| F12 | 托盘控制 | P0 | 右键菜单：显隐、锁定广告态、暂停轮播、设置、退出 |
| F13 | 悬停迷你工具条 | P2 | 悬停 1s 后浮现半透明控制条（切上/下、暂停、静音、关闭→隐藏） |
| F14 | 开机自启 | P2 | 注册表 Run 键，静默启动进广告态 |

### 2.2 非功能需求

- **隐蔽性**：窗口样式 `WS_EX_TOOLWINDOW`（不进任务栏/Alt+Tab）；默认标题"广告推广"，托盘 Tooltip"广告服务"；进程名中性。
- **可靠性**：所有异常静默捕获写日志，不弹错误框；单实例运行（命名互斥体）。
- **隐私与合规**：不上报任何数据；仅加载平台官方嵌入播放器；默认关闭登录功能。
- **兼容性**：PerMonitorV2 DPI 感知；多显示器；任务栏自动隐藏场景下正确定位。

### 2.3 核心用户故事

1. 小王上班时按 `Ctrl+Alt+V`，右下角浮现"广告弹窗"在轮播；鼠标移过去，广告淡出、B 站视频继续播；领导路过，鼠标移开——视频瞬间暂停，画面回到广告。
2. 用 `Ctrl+Alt+→` 切到下一个短视频，`Ctrl+Alt+↑/↓` 调音量，全程不用碰窗口。
3. 领导突然出现，按 `Ctrl+Alt+H` 老板键，窗口 100ms 内消失（或点"广告"右上角的假 ✘，等效老板键）。

---

## 3. 技术选型

### 3.1 选型结论

| 类别 | 方案 | 理由 |
| --- | --- | --- |
| 语言/运行时 | C# / .NET 8 | Win32 互操作（热键、鼠标轮询、窗口样式）最顺手，单框架 |
| UI 框架 | WPF | 成熟稳定，无边框窗口/透明/托盘生态完善 |
| 视频承载 | **Microsoft WebView2**（单一实例复用） | 平台视频必须在网页播放器中放；WebView2 随 Win11 内置，Win10 自动装 |
| 伪装层 | WPF 原生渲染（Image + Border） | 广告轮播不进浏览器，切换快、开销低 |
| 全局热键 | Win32 `RegisterHotKey` | 官方标准，窗口未聚焦也生效 |
| 鼠标检测 | `DispatcherTimer` + `GetCursorPos` 轮询（100ms） | WebView2 是独立 HWND，内部鼠标事件不冒泡到 WPF，轮询最稳 |
| 本地数据 | JSON 文件（`System.Text.Json`） | 数据量小，免数据库依赖；含上次播放位置 |
| 日志 | Serilog（滚动文件，≤10MB×3） | 排障用，可一键关闭 |
| 打包 | Inno Setup / 单文件自包含发布 | 免安装单 exe + WebView2 检测引导 |

### 3.2 备选方案对比（评审用）

| 方案 | 优点 | 缺点 | 结论 |
| --- | --- | --- | --- |
| **C# WPF + WebView2**（选定） | 热键/托盘/窗口控制原生；伪装层与播放层分离简单 | 仅 Windows | ✅ 采用 |
| Electron | Web 生态成熟 | 包体 150MB+、内存高，伪装层也要塞进浏览器 | ❌ |
| Tauri 2 | 包体小 | 需要 Rust；多 WebView/托盘生态较弱 | ❌ |
| WinForms + WebView2 | 简单 | 无边框/透明/DPI 细节不如 WPF | ❌ |

### 3.3 关键技术可行性说明

- **播放方式**：哔哩哔哩/腾讯视频提供官方 iframe 嵌入播放器，爱奇艺有开放平台嵌入，合规且稳定；抖音无公开嵌入播放器，采用"网页适配"（WebView2 直接加载视频页 + CSS 注入隐藏页面 UI），列为 P1，另有"解析 CDN 直链"备选（有失效与合规风险，默认不启用）。
- **自动播放策略**：浏览器默认禁止无手势自动播放。WebView2 通过环境参数 `--autoplay-policy=no-user-gesture-required` 放行，保证"悬停即播"。
- **悬停检测**：WebView2 内容区是独立 HWND，鼠标事件不透传到 WPF；采用 100ms `GetCursorPos` 轮询判定光标是否在窗口矩形内（`WindowFromPoint` 辅助校验置顶遮挡情况），简单可靠、CPU 开销可忽略。

---

## 4. 系统架构设计

### 4.1 总体架构

```
┌────────────────────────────────────────────────────────┐
│                     弹窗窗口 (WPF, 无边框)               │
│  ┌────────────────────────────┐  ┌───────────────────┐ │
│  │  伪装层 AdLayer (WPF)       │  │ 播放层             │ │
│  │  - 广告轮播控件             │  │ WebView2 (单实例) │ │
│  │  - 假关闭按钮(✘=老板键)     │  │  - 视频源适配     │ │
│  │  - 假进度点/假倒计时        │  │  - 迷你工具条     │ │
│  └────────────────────────────┘  └───────────────────┘ │
│                    两层按状态互斥显示                    │
└──────────────┬─────────────────────────┬───────────────┘
               │                         │
┌──────────────▼─────────────────────────▼───────────────┐
│                        核心层 Core                       │
│  AppStateMachine   MouseWatcher   HotkeyManager         │
│  PlaybackController(→SourceAdapter)  PlaylistManager    │
│  ConfigManager   EventBus   SingleInstanceGuard         │
└──────────────┬─────────────────────────────────────────┘
               │
┌──────────────▼─────────────────────────────────────────┐
│                      数据层 Data                         │
│   config.json   playlist.json   history.json   ads/     │
│   (%APPDATA%\MoyuPopup)      Serilog 日志               │
└─────────────────────────────────────────────────────────┘
        外围：托盘(NotifyIcon)  设置窗口  登录窗口(P1)
```

### 4.2 模块职责

| 模块 | 职责 |
| --- | --- |
| `AdLayer` | 广告轮播渲染（淡入淡出、轮播间隔、假 ✘ / 假进度点），**纯 WPF，不联网** |
| `PlayerHost` | 唯一 WebView2 实例的宿主；切源=换 URL，避免多 WebView 内存叠加 |
| `MouseWatcher` | 100ms 轮询光标位置，产出 `MouseEnter/MouseLeave` 事件 |
| `AppStateMachine` | 维护 `Hidden / Ad / Playing` 三态及迁移（见第 9 章） |
| `HotkeyManager` | `RegisterHotKey` 封装，注册失败降级提示 |
| `PlaybackController` | 播放/暂停/切集/音量；调用 `SourceAdapter` 产出目标 URL 并向 WebView2 注入 JS |
| `SourceAdapter(平台适配器)` | 各平台 URL 解析 → 嵌入地址/适配页 + 播放控制 JS 片段 |
| `PlaylistManager` | 队列、历史、位置记忆、去重 |
| `TrayController` | 托盘图标、右键菜单、气泡提示 |
| `ConfigManager` | 配置读写 + 防损坏兜底（坏文件自动回默认并备份） |

### 4.3 线程模型

- **UI 线程**：WPF 渲染、WebView2 交互（WebView2 必须 UI 线程操作）。
- **热键**：`WM_HOTKEY` 消息在 UI 线程窗口过程中处理（本来就是轻量消息）。
- **鼠标轮询**：`DispatcherTimer`(100ms) 于 UI 线程，仅做矩形判定，无阻塞风险。
- **I/O（配置/队列/日志）**：`async/await` + `Task.Run`，避免 UI 卡顿。

---

## 5. 核心机制详细设计

### 5.1 弹窗窗口（PopupWindow）

- **样式**：`WindowStyle=None`、`AllowsTransparency=true`、圆角 8px、阴影、默认 `Topmost=true`（可设置）、`ShowInTaskbar=false`。
- **Win32 扩展样式**：`WS_EX_TOOLWINDOW` —— 不出现在任务栏与 Alt+Tab 列表（隐蔽核心点之一）。
- **定位逻辑**：
  1. 首次启动：取**主屏工作区**（`Screen.WorkingArea`，自动避开任务栏），右下角留白 `margin=24px`；
  2. 若工作区高度大于 1.2 倍主屏高（任务栏自动隐藏等场景），按屏幕底部减 margin 处理；
  3. 位置持久化到配置；多显示器下记录屏幕设备名，拔掉副屏时回退主屏右下角。
- **拖拽**：默认禁止拖动以防误触；按住 `Ctrl + 左键拖动` 才可移动（也可在设置中改为标题区拖动）。
- **尺寸**：预设 320×180 / 400×225 / 480×270 / 640×360，可自定义，宽高比锁定 16:9（也可解锁）。

### 5.2 双层内容引擎（伪装层 / 播放层）

两图层同尺寸叠加，按状态互斥显隐：

| 状态 | 伪装层 | 播放层 |
| --- | --- | --- |
| Ad（伪装态） | 显示（轮播中） | `Visibility=Collapsed`（保留实例，切回更快） |
| Playing（播放态） | 隐藏 | 显示 |

- 切换动画：150ms 淡入淡出（`Opacity` 过渡），避免生硬闪烁引人注意。
- **为什么伪装层不用 WebView**：纯 WPF 渲染启动快、内存低、老板键时无需销毁浏览器页面。

### 5.3 广告轮播（AdCarousel）

- **素材来源**：`%APPDATA%\MoyuPopup\ads\` 下的本地图片（jpg/png/webp），内置 6 张默认"合成假广告"（电商大促风：倒计时、立即抢购按钮等，由 HTML 设计稿导出图片随包分发，不联网加载）。
- **轮播逻辑**：默认 5s/张，`CrossFade` 500ms 过渡；顺序随机（避免每次都是同一张，更像真实轮播）；可配置轮播间隔与是否随机。
- **仿真细节（提升可信度）**：
  - 右上角假关闭按钮 ✘ —— **点击 = 触发老板键**（隐藏窗口），符合用户对广告弹窗的肌肉记忆，同时实现隐蔽；
  - 底部进度点（当前第几张/共几张）；
  - 角标"广告"字样（模拟信息流广告标识）；
  - 可选假倒计时（"距结束 00:59"随机递减）。
- **素材缺失兜底**：目录为空/图片损坏时，回退内置纯色+文字模板（"限时推广"），保证永远有内容显示。

### 5.4 悬停播放机制（MouseWatcher）

```
DispatcherTimer(100ms):
    pos = GetCursorPos()
    inside = 窗口Rect.Contains(pos) && WindowFromPoint(pos) 属于本窗口树
    事件: 持续 inside ≥ hoverDelayMs → MouseEnter
          once 离开 → MouseLeave
```

- **防误触延时**：`hoverDelayMs`（默认 300ms）——光标只是路过时不触发播放，必须**停留**才切换。
- **移开即停**：`MouseLeave` → 立即暂停视频（`PlaybackController.Pause()`）→ 150ms 淡出回广告态。
- **行为开关**（设置项）：
  - `pauseOnLeave`（默认 true）：移开是否暂停；关闭则继续播放只缩回广告画面（出声，风险自担）；
  - `lockAdMode`：**上班锁定模式**——开启后悬停不播放，恒为广告态（一键切换，热键 `Ctrl+Alt+L`）。
- **全屏播放**：WebView2 内全屏（双击视频）仍在本窗口内实现，光标移出窗口矩形依旧触发暂停；全屏状态下 `MouseLeave` 先退出全屏再回广告态。

### 5.5 播放控制（PlaybackController）

- **播放/暂停**：统一走 **JS 注入**，由 `SourceAdapter` 提供平台专属 JS 片段（如 B 站嵌入播放器暴露的 `postMessage` API、通用方案则派发键盘事件 `Space`）；无原生 API 的平台降级为"模拟按键"。
- **自动播放**：WebView2 环境参数 `--autoplay-policy=no-user-gesture-required` + 嵌入 URL 携带 `autoplay=1`。
- **切集**：`Next()/Prev()` 遍历队列 → 换嵌入 URL → 重置悬停态为 Playing；位置记忆（>30s 记录 `lastPositionSec`，切回续播）。
- **音量**：热键/工具条调节；默认 40%（防止突然出声）。
- **失败处理**：页面加载超时（10s）→ 托盘气泡提示"该视频暂不可播"→ 自动跳下一集；连续 3 次失败暂停队列并回广告态。

### 5.6 全局热键（HotkeyManager）

- 基于 `RegisterHotKey` + `WM_HOTKEY`；注册冲突时依次尝试备用组合，仍失败则气泡提示并引导修改。
- **热键表（默认，可配置）**：

| 热键 | 动作 | 说明 |
| --- | --- | --- |
| `Ctrl+Alt+V` | 显示 / 隐藏（老板键） | 隐藏=窗口整体消失，恢复后回广告态 |
| `Ctrl+Alt+→` | 下一集 | Ad 态下也可用（切换后进入 Playing） |
| `Ctrl+Alt+←` | 上一集 | |
| `Ctrl+Alt+Space` | 播放 / 暂停 | |
| `Ctrl+Alt+↑ / ↓` | 音量 +10% / -10% | |
| `Ctrl+Alt+L` | 上班锁定（恒广告态）开关 | |
| `Ctrl+Alt+S` | 截取当前视频帧到队列备注 | P2 |

- 窗口悬停期间的**窗口内快捷键**（不注册全局，避免冲突）：`N/P` 切集、`←/→` 快进退 5s、`M` 静音，经 WebView2 派发或 JS 注入。

### 5.7 托盘与隐蔽性设计

| 项目 | 设计 |
| --- | --- |
| 任务栏 | 无图标（`ShowInTaskbar=false` + `WS_EX_TOOLWINDOW`） |
| Alt+Tab | 不可见（同上） |
| 窗口标题 | 默认"广告推广"，可配置 |
| 托盘图标 | 默认显示中性图标，Tooltip"广告服务"；可设置"隐藏托盘图标"（`NotifyIcon.Visible=false`，仅热键操作，防手滑找不到，二次确认） |
| 进程名 | `AdPopup.exe`（中性命名） |
| 老板键 | 隐藏窗口 + 强制暂停视频 + 清空 WebView2 前进栈痕迹（可选） |
| 隐私 | 零上报；历史/队列/配置全本地 |

### 5.8 登录态支持（P1）

- 独立"登录窗口"承载 WebView2，与播放层使用**同一 `UserDataFolder`**（`%APPDATA%\MoyuPopup\WebView2`），Cookie 自动共享。
- 支持扫码登录 B 站 / 抖音 / 腾讯视频，登录后嵌入播放器可用 VIP 内容（平台策略允许范围内）。
- 默认关闭该功能；开启后设置页明示"登录信息仅保存在本机"。
- DRM 说明：WebView2 支持平台常规 DRM 内容；个别站点检测 WebView2 环境时降级为"网页适配"模式打开。

---

## 6. 界面设计

### 6.1 弹窗窗口布局（400×225 默认）

```
┌─────────────────────────────────┐
│ ✘(假关闭=老板键)        [广告]角标│   ← 伪装态：轮播广告
│                                 │
│        （广告素材图）             │
│                                 │
│   距结束 00:59    ● ○ ○ ○ ○     │   ← 假倒计时 + 进度点
└─────────────────────────────────┘
             ↓ 鼠标悬停 300ms
┌─────────────────────────────────┐
│                                 │
│      （WebView2 视频画面）        │   ← 播放态
│                                 │
│  ⏮ ⏯ ⏭  🔊──  [隐藏] [×]      │   ← 悬停 1s 浮现迷你工具条
└─────────────────────────────────┘
```

### 6.2 设置窗口

- 常规：开机自启、启动进广告态、托盘隐藏。
- 窗口：尺寸、透明度、置顶开关、悬停延时、移开暂停开关。
- 热键：全部热键可视化录制与冲突检测。
- 视频源：平台开关、默认源、登录入口。
- 广告：素材目录打开、轮播间隔、随机顺序。
- 关于：版本、免责声明。

### 6.3 视觉规范

- 圆角 8px，边框 1px `#E0E0E0`，阴影 `Blur=24, Opacity=0.35`（贴近系统弹窗观感）。
- 迷你工具条：高 36px，半透明黑底白字，悬停 1s 淡入、3s 无操作淡出。

---

## 7. 视频源适配器设计

### 7.1 适配器接口

```csharp
/// <summary>视频源适配器：将用户输入的分享链接解析为可播放的嵌入地址与控制脚本</summary>
public interface IVideoSourceAdapter
{
    /// <summary>平台标识，如 bilibili / douyin / tencent / iqiyi / custom</summary>
    string Platform { get; }

    /// <summary>判断该适配器能否处理此 URL</summary>
    bool CanParse(string url);

    /// <summary>解析输入链接（支持普通页、分享短链、BV/vid 纯 ID）</summary>
    Task<VideoItem> ParseAsync(string url, CancellationToken ct);

    /// <summary>返回 WebView2 环境附加参数（如自动播放策略）</summary>
    IEnumerable<string> BrowserArgs { get; }

    /// <summary>获取"播放"JS 片段（空串表示依赖自动播放）</summary>
    string BuildPlayJs(VideoItem item);

    /// <summary>获取"暂停"JS 片段</summary>
    string BuildPauseJs(VideoItem item);
}
```

### 7.2 平台适配矩阵

| 平台 | 优先级 | 接入方式 | 输入示例 | 产出嵌入地址 | 风险/备注 |
| --- | --- | --- | --- | --- | --- |
| 哔哩哔哩 | P0 | **官方 iframe 嵌入** | `b23.tv/xxx`、`BV1xx...` | `player.bilibili.com/player.html?bvid=…&autoplay=1&danmaku=0` | 官方支持，稳定合规 |
| 腾讯视频 | P0 | 官方 iframe 嵌入 | `v.qq.com/x/cover/…`、vid | `v.qq.com/txp/iframe/player.html?vid=…&auto=1` | 免费内容可用 |
| 自定义 URL | P0 | 直接加载 | 任意网页 | 原样加载 | 通用兜底 |
| 抖音 | P1 | 网页适配（加载视频页 + CSS 注入隐藏 UI） | `v.douyin.com/xxx`、`/video/{id}` | `www.douyin.com/video/{id}` + 注入样式 | 页面改版需跟进；CDN 直链解析方案默认关闭 |
| 爱奇艺 | P1 | 开放平台嵌入 / 网页适配 | `iqiyi.com/v_xxx.html` | 开放平台 player 或页面适配 | 部分内容受限 |
| 斗鱼/抖音直播 | P2 | 网页适配 | 直播间链接 | 直播页适配 | P2 阶段评估 |

### 7.3 适配器注册机制

- 启动时按配置中的 `sources.enabled` 顺序注册 `List<IVideoSourceAdapter>`；
- `PlaybackController` 收到 URL 后按注册顺序找第一个 `CanParse()==true` 的适配器；
- 新增平台 = 新增一个适配器类 + 配置项，核心层零改动（满足可扩展性目标）。

---

## 8. 数据存储设计

### 8.1 目录结构

```
%APPDATA%\MoyuPopup\
├── config.json          # 应用配置
├── playlist.json        # 播放队列
├── history.json         # 播放历史（最近 500 条，滚动清理）
├── ads\                 # 广告素材目录（用户可放图）
├── WebView2\            # WebView2 用户数据（Cookie/缓存，登录态）
└── logs\                # Serilog 滚动日志
```

### 8.2 config.json 结构

```json
{
  "window": {
    "width": 400, "height": 225, "margin": 24,
    "opacity": 0.95, "topmost": true,
    "position": { "screen": "\\\\.\\DISPLAY1", "x": 1490, "y": 780 },
    "sizeLocked": true
  },
  "behavior": {
    "hoverDelayMs": 300,
    "pauseOnLeave": true,
    "lockAdMode": false,
    "adIntervalSec": 5,
    "adShuffle": true,
    "defaultVolume": 40,
    "rememberPosition": true
  },
  "stealth": {
    "windowTitle": "广告推广",
    "trayVisible": true,
    "trayTooltip": "广告服务",
    "bossKeyAction": "Hide"
  },
  "hotkeys": {
    "toggle": "Ctrl+Alt+V",
    "next": "Ctrl+Alt+Right",
    "prev": "Ctrl+Alt+Left",
    "playPause": "Ctrl+Alt+Space",
    "volUp": "Ctrl+Alt+Up",
    "volDown": "Ctrl+Alt+Down",
    "lockAd": "Ctrl+Alt+L"
  },
  "sources": {
    "enabled": ["bilibili", "tencent", "custom", "douyin", "iqiyi"],
    "loginEnabled": false
  },
  "startup": { "autoStart": false, "startInAdMode": true }
}
```

### 8.3 playlist.json 条目结构

```json
{
  "version": 1,
  "items": [
    {
      "id": "b0c4e...",
      "platform": "bilibili",
      "title": "【自制】某某视频",
      "inputUrl": "https://b23.tv/xxxx",
      "embedUrl": "https://player.bilibili.com/player.html?bvid=BV...&autoplay=1",
      "tags": ["搞笑"],
      "addedAt": "2026-09-07T12:00:00",
      "lastPositionSec": 45,
      "durationSec": 213
    }
  ],
  "order": ["b0c4e..."],
  "currentIndex": 0
}
```

### 8.4 容错策略

- 读写全部 `try/catch`；文件损坏 → 备份为 `*.bad` 并回退默认值；
- 保存采用"写临时文件 + 原子替换"，防止断电损坏；
- 队列去重：按 `embedUrl` 归一化后的哈希判重。

---

## 9. 状态机设计

### 9.1 状态定义

| 状态 | 含义 | 画面 | 视频 |
| --- | --- | --- | --- |
| `Hidden` | 老板键隐藏 | 窗口不可见 | 强制暂停 |
| `Ad` | 广告伪装态 | 轮播广告 | 暂停 |
| `Playing` | 悬停播放态 | 视频 | 播放中 |

### 9.2 状态转移

```
                 ┌────────────── 老板键(Hide) ──────────────┐
                 │                                          ▼
   ┌─────────┐ 悬停≥300ms ┌─────────┐                 ┌──────────┐
   │   Ad    │──────────▶│ Playing │                 │  Hidden  │
   │ (默认态) │◀──────────│         │                 └──────────┘
   └─────────┘ 移开即停   └─────────┘                     ▲
        │    ▲              │      ▲                     │
        │    └─ lockAdMode ─┘      │ 热键 Next/Prev      │ 老板键
        │    （悬停不播放）          │ （Ad 态亦可切集）     │
        │                          ▼                     │
        └──── 热键 Next/Prev ──▶ (进入 Playing) ──────────┘
   Hidden ──老板键(Show)──▶ Ad
```

### 9.3 转移规则补充

- `Ad → Playing`：仅当 `lockAdMode=false` 且悬停时长 ≥ `hoverDelayMs`。
- `Playing → Ad`：光标离开窗口矩形；若全屏中先退出全屏。
- 任意态 `→ Hidden`：老板键 / 假 ✘ 点击；`Hidden → Ad`：老板键恢复（**恢复后必回广告态**，不直接播放，安全第一）。
- 热键切集在 `Ad` 态可用：直接换源并进入 `Playing`（用户主动行为，视为非误触）。

---

## 10. 关键流程时序设计

### 10.1 悬停播放 / 移开暂停

```
MouseWatcher        StateMachine      AdLayer/PlayerHost      WebView2
    │  Enter(300ms)      │                  │                   │
    │──────────────────▶ │ Ad→Playing       │                   │
    │                    │ 淡出广告(150ms)   │                   │
    │                    │────────────────▶ │ Show WebView2     │
    │                    │                  │──BuildPlayJs()──▶ │ 播放
    │  Leave             │                  │                   │
    │──────────────────▶ │ Playing→Ad       │                   │
    │                    │──────────────────│────PauseJs()────▶│ 暂停
    │                    │ 淡入广告(150ms)   │                   │
```

### 10.2 全局热键切换（下一集）

```
Win32(WM_HOTKEY)   PlaybackController   PlaylistManager    WebView2
    │ Next                │                  │               │
    │────────────────────▶│ currentIndex+1   │               │
    │                     │─────────────────▶│ 取 item       │
    │                     │ embedUrl=adapter │               │
    │                     │────────────────────────────────▶ │ Navigate
    │                     │ 状态置 Playing，工具条刷新标题      │
```

### 10.3 老板键

```
Win32 / 假✘点击 ──▶ StateMachine: *→Hidden
                    ① PauseJs()（≤50ms）
                    ② Window.Hide()（≤100ms 内完成）
                    ③ 托盘图标状态更新
恢复: Show() ──▶ 回 Ad 态轮播
```

---

## 11. 安全与合规

| 维度 | 设计 |
| --- | --- |
| 播放合规 | **仅使用平台官方提供/公开的嵌入播放器**；不去广告、不破解、不下载、不缓存视频流 |
| VIP 内容 | 需用户自行登录账号，遵守平台会员协议；未登录仅免费内容 |
| 抖音网页适配 | 属"浏览器正常访问页面"范畴；CDN 直链解析方案默认关闭，仅在评估合规后可选开启 |
| 隐私 | 零网络上报；Cookie/历史全本地；日志默认记录级别 Information 且不含 URL 参数 |
| 用途边界 | 软件内置首次启动《使用须知》：用户应遵守所在组织规章制度，因不当使用产生的后果由用户自行承担 |
| 代码安全 | 不注入他人进程、不钩键盘（仅 `RegisterHotKey`）、不做任何反检测对抗 |

---

## 12. 性能与资源占用规范

| 指标 | 目标 | 手段 |
| --- | --- | --- |
| 常驻内存 | 主进程 ≤ 60MB；含 WebView2 子进程 ≤ 250MB | 单 WebView 实例复用；广告层纯 WPF |
| 空闲 CPU | ≤ 2% | 广告轮播用 `CompositionTarget` 定时淡入淡出；轮询 100ms 仅矩形判定 |
| 状态切换 | ≤ 150ms | 广告层与播放层仅切 Visibility，不销毁页面 |
| 老板键响应 | ≤ 100ms | 消息直处理，无异步链路 |
| 启动时间 | 冷启动 ≤ 2s | WebView2 懒加载（首次切到 Playing 才初始化） |
| 安装包 | ≤ 30MB（不含 WebView2 Runtime） | 自包含裁剪发布；缺 Runtime 时引导安装 Evergreen Bootstrapper |

---

## 13. 测试计划

### 13.1 单元测试

- 状态机全部合法/非法转移覆盖；
- `PlaylistManager`：去重、排序、位置记忆、损坏文件兜底；
- 各 `SourceAdapter` 的 URL 解析（普通页/短链/纯 ID/非法输入）；
- 热键解析与冲突降级逻辑。

### 13.2 集成测试（手工为主）

- 悬停 → 播放 → 移开 → 暂停回广告，全链路 50 次无失败、无内存增长；
- 老板键连按 100 次，窗口显隐与视频状态一致；
- 多显示器插拔、DPI 100%/150%/200%、任务栏自动隐藏场景下定位正确；
- WebView2 未安装机器的引导安装流程。

### 13.3 稳定性测试

- 24h 常驻：内存曲线平稳（无持续增长）、无崩溃；
- 广告目录含超大图片（4K PNG）不卡 UI（解码放后台）；
- 断网/平台页 404/视频下架：10s 超时 → 跳集，不白屏卡死。

### 13.4 验收清单（核心用例）

| 用例 | 预期 |
| --- | --- |
| 启动后不操作 | 右下角广告轮播，无声音，任务栏/Alt+Tab 无痕迹 |
| 鼠标路过窗口（<300ms） | 不切换、不播放 |
| 悬停 300ms | 150ms 内显示视频并开始播放 |
| 移开鼠标 | ≤300ms 内暂停并回到广告轮播 |
| 按老板键 | ≤100ms 隐藏；恢复后为广告态 |

---

## 14. 开发计划与里程碑

| 里程碑 | 内容 | 验收标准 |
| --- | --- | --- |
| M1 骨架 | 无边框窗口 + 右下角定位 + 广告轮播 + 托盘 + 老板键 | 用例 13.4 前两条通过 |
| M2 播放核心 | WebView2 集成 + B 站/腾讯/自定义适配器 + 悬停播放暂停 | 用例 13.4 全部通过 |
| M3 热键与队列 | 全局热键全套 + 播放队列/历史/位置记忆 | 热键全流程可用 |
| M4 体验完善 | 设置界面 + 登录窗口 + 抖音/爱奇艺适配 + 迷你工具条 | P1 功能可用 |
| M5 发布 | 打包、自动更新（Velopack）、使用须知、签名 | 安装包可分发 |

---

## 15. 风险识别与应对

| 风险 | 等级 | 应对 |
| --- | --- | --- |
| 平台嵌入播放器策略变更（B 站/腾讯 iframe 参数调整） | 高 | 适配器隔离变更面；内置"自定义 URL"兜底；快速热更新 |
| 抖音页面改版导致网页适配失效 | 中 | 适配器版本化管理；CSS 注入规则远程可配置（P2）；失效自动降级提示 |
| WebView2 Runtime 未安装（老 Win10） | 中 | 首次运行检测并引导官方 Bootstrapper 静默安装 |
| 自动播放被平台限制 | 中 | `--autoplay-policy` 环境参数 + 嵌入参数 `autoplay=1` 双保险；仍失败时模拟点击播放按钮 |
| 热键与其他软件冲突 | 低 | 注册失败自动换备选组合并气泡提示 |
| 目标环境禁装软件/企业安全软件告警 | 低 | 不做对抗；单文件绿色版 + 用户自担 |
| 误触导致声音外放 | 低 | 默认音量 40%；可选默认静音 |

---

## 16. 附录：接口定义、热键表与配置示例

### 16.1 核心接口（C#）

```csharp
/// <summary>播放控制器：对外暴露统一的播放控制，屏蔽平台差异</summary>
public interface IPlaybackController
{
    /// <summary>当前条目（Ad 态下可能为 null）</summary>
    VideoItem? Current { get; }

    Task PlayAsync(VideoItem item, CancellationToken ct);
    Task PlayOrPauseAsync();   // 依据平台适配器选择 postMessage / 模拟按键
    Task PauseAsync();
    Task NextAsync(int offset = 1);   // 正/负偏移，支持上一集/下一集
    Task SetVolumeAsync(int percent); // 0-100
}

/// <summary>状态机</summary>
public interface IAppStateMachine
{
    AppStatus Current { get; }                       // Hidden / Ad / Playing
    event Action<AppStatus, AppStatus> Transitioned; // (old, new)
    void Fire(AppEvent evt);                         // MouseEnter/Leave/Hide/Show/...
}

/// <summary>热键服务</summary>
public interface IHotkeyService
{
    bool Register(HotkeyCombo combo, Action callback); // 失败返回 false（冲突）
    void UnregisterAll();
}

/// <summary>鼠标监视器</summary>
public interface IMouseWatcher
{
    event Action Entered;  // 悬停超过 hoverDelayMs 后触发
    event Action Left;     // 离开窗口矩形立即触发
}
```

### 16.2 事件枚举

```csharp
public enum AppEvent
{
    MouseEnter,      // 悬停达标
    MouseLeave,      // 光标离开
    BossKey,         // 老板键（隐藏）
    Restore,         // 恢复显示
    NextItem,        // 下一集
    PrevItem,        // 上一集
    LockAdToggle     // 上班锁定开关
}
```

### 16.3 默认广告素材规范

- 格式：JPG/PNG/WebP，建议 800×450（2x 高清适配 400×225）；
- 命名：`ad_01.jpg` 起，程序按文件名排序；
- 内置 6 张默认素材随包分发（合成电商风格，不联网）。

---

## 附：待评审决策点

1. 抖音接入是否启用"CDN 直链解析"备选方案（合规需评估，默认关闭）；
2. 托盘图标是否支持"完全隐藏"（已列入 P1，需二次确认防丢失）；
3. 是否需要"双显示器跟随鼠标所在屏"的停靠策略（当前仅主屏）；
4. 自动更新渠道（Velopack GitHub / 自托管）。

> 本文档为设计稿 v1.0，评审通过后进入 M1 开发。
